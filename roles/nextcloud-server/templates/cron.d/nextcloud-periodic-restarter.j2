MAILTO="{{ ssl_support_email }}"

# This periodically restarts the Nextcloud services
# to ensure they're using the latest SSL certificate
# in case it got renewed by the `ssl-certificate-renewal` cronjob
# (which happens once every ~2-3 months).
#
# Because `nextcloud-nginx-proxy.service` depends on `nextcloud-apache.service`,
# both would be restarted.

{{ nextcloud_services_restart_cron_time_definition }} root /usr/bin/systemctl restart nextcloud-apache.service
