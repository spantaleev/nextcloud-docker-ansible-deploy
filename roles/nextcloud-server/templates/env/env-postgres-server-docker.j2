POSTGRES_USER={{ nextcloud_postgres_connection_username }}
POSTGRES_PASSWORD={{ nextcloud_postgres_connection_password }}
# We intentionally point this to another database.
# See https://github.com/spantaleev/nextcloud-docker-ansible-deploy/issues/2
POSTGRES_DB=another
