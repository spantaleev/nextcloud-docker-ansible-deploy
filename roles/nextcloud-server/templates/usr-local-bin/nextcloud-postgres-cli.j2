#!/bin/bash

docker run \
	-it \
	--rm \
	--env-file={{ nextcloud_environment_variables_data_path }}/env-postgres-pgsql-docker \
	--link=nextcloud-postgres:postgres \
	{{ nextcloud_docker_image_postgres_to_use }} \
	psql -h postgres
