#jinja2: lstrip_blocks: "True", trim_blocks: "True"

location:
  source_directories: {{ nextcloud_backup_borg_location_source_directories|to_json }}
  repositories: {{ nextcloud_backup_borg_location_repositories|to_json }}
  one_file_system: true
  exclude_patterns: {{ nextcloud_backup_borg_location_exclude_patterns|to_json }}

storage:
  compression: {{ nextcloud_backup_borg_storage_compression|to_json }}
  ssh_command: {{ nextcloud_backup_borg_storage_ssh_command|to_json }}
  archive_name_format: {{ nextcloud_backup_borg_storage_archive_name_format|to_json }}
  encryption_passphrase: {{ nextcloud_backup_borg_storage_encryption_passphrase|to_json }}
  unknown_unencrypted_repo_access_is_ok: {{ nextcloud_backup_borg_unknown_unencrypted_repo_access_is_ok|to_json }}

retention:
  keep_hourly: {{ nextcloud_backup_borg_retention_keep_hourly|to_json }}
  keep_daily: {{ nextcloud_backup_borg_retention_keep_daily|to_json }}
  keep_weekly: {{ nextcloud_backup_borg_retention_keep_weekly|to_json }}
  keep_monthly: {{ nextcloud_backup_borg_retention_keep_monthly|to_json }}
  keep_yearly: {{ nextcloud_backup_borg_retention_keep_yearly|to_json }}
  prefix: {{ nextcloud_backup_borg_retention_prefix|to_json }}

consistency:
  checks:
    - repository
    - archives

hooks:
{% if nextcloud_backup_borg_postgresql_enabled and nextcloud_backup_borg_postgresql_databases|length > 0 %}
  postgresql_databases:
  {% for database in nextcloud_backup_borg_postgresql_databases %}
  - name: {{ database|to_json }}
    hostname: {{ nextcloud_backup_borg_postgresql_databases_hostname|to_json }}
    username: {{ nextcloud_backup_borg_postgresql_databases_username|to_json }}
    password: {{ nextcloud_backup_borg_postgresql_databases_password|to_json }}
    port: {{ nextcloud_backup_borg_postgresql_databases_port | int | to_json }}
  {% endfor %}
{% endif %}
  after_backup:
    - echo "Backup created."
  on_error:
    - echo "Error while creating a backup."
