---

- ansible.builtin.import_tasks: tasks/setup_onlyoffice_document_server.yml
  tags:
    - setup-all
    - setup-onlyoffice-document-server

# This is meant to be executed subsequently, after Nextcloud is fully setup
- ansible.builtin.import_tasks: tasks/setup_onlyoffice_app.yml
  tags:
    - setup-onlyoffice-app
