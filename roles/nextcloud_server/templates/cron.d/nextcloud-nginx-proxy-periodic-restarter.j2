MAILTO="{{ nextcloud_ssl_support_email }}"

# This periodically reloads the nextcloud-nginx-proxy service
# to ensure it's using the latest SSL certificate
# in case it got renewed by the `nextcloud-ssl-certificate-renewal` cronjob
# (which happens once every ~2-3 months).

{{ nextcloud_services_restart_cron_time_definition }} root /usr/bin/systemctl reload nextcloud-nginx-proxy.service
